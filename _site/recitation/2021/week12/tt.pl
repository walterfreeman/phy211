for ($x = -9; $x < 9; $x += 0.01)
{
	$th = ($x+9) / 18 * 3.14159;
	printf "%e %e\n",$x,sin($th)*1.2;
}

for ($x = -9; $x < 9; $x += 0.3)
{
	$th = ($x+9) / 18 * 3.14159;
	$th2 = ($x+0.1+9) / 18 * 3.14159;
	printf "line %e %e %e %e\n",$x,-sin($th)*1.2,($x + 0.1),-sin($th2)*1.2;
}
